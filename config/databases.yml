test:
  doctrine:
    param:
      dsn:      sqlite::memory

all:
  doctrine:
    class: sfDoctrineDatabase
    param:
      dsn:      mysql:host=localhost;dbname=sl_movie_store
      username: root
      password: ~
